---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: fu4-test
  host: fu4-test
  memory: 512M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry
  no-route: true
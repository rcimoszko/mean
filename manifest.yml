---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: fu-production
  host: fu-production
  memory: 512M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry
---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: fu-stage
  host: fu-stage
  memory: 512M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry-stage